funcao __somainteiro(inteiro !a, inteiro !b) {
    retorno !a ;
}

funcao __vazia() {
    escreva("Função vazia chamada");
}

funcao __principal() {
    inteiro !x = 10;
    inteiro !y

    !resultado = __soma(!x, !y);

    escreva("O resultado da soma é: ", !resultado);

    !x ++;
    !x <= !y;
    !x >= !y;
}
